package a

import (
	"github.com/tbeati/stacked"
)

func assignmentSelf() {
	var err error
	_ = err

	err = singleReturn()
	_, err = multipleReturn()

	localFuncSingle := singleReturn
	err = stacked.Wrap(localFuncSingle()) // want "^error returned by localFuncSingle is not wrapped with stacked$"
	err = stacked.Wrap(localFuncSingle())

	localFuncMultiple := multipleReturn
	_, err = stacked.Wrap2(localFuncMultiple()) // want "^error returned by localFuncMultiple is not wrapped with stacked$"
	_, err = stacked.Wrap2(localFuncMultiple())

	s := structWithMethods{}
	err = s.singleReturn()
	_, err = s.multipleReturn()

	var i localInterface
	err = stacked.Wrap(i.SingleReturn()) // want "^error returned by i.SingleReturn is not wrapped with stacked$"
	err = stacked.Wrap(i.SingleReturn())
	_, err = stacked.Wrap2(i.MultipleReturn()) // want "^error returned by i.MultipleReturn is not wrapped with stacked$"
	_, err = stacked.Wrap2(i.MultipleReturn())

	err = stacked.Wrap(errGlobal) // want "^errGlobal is not wrapped with stacked$"
	err = stacked.Wrap(errGlobal)

	err = stacked.Wrap(stringError("error")) // want "^value converted to error type stringError is not wrapped with stacked$"
	err = stacked.Wrap(stringError("error"))

	err = stacked.Wrap(structError{message: "error"}) // want "^structError literal is not wrapped with stacked$"
	err = stacked.Wrap(structError{message: "error"})
}
